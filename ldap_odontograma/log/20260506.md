auth servicio docs ldapts auditoria refactor apigateway errorlog jsonl middleware routes
Resumen inicial: Registro de cambios realizados el 2026-05-06 — numeración, documentación y auditoría.

- 2026-05-06 12:18:10 Dev solicita: Inserta los números de línea en el MD para que coincidan con el TXT (`docs/Oscar/auth.service.numbered.txt`).
- 2026-05-06 12:18:10 Dev realiza: Reemplazado el bloque "Código Fuente (con números de línea)" en `docs/Oscar/auth.service.js.md` con la numeración oficial (1..638) tomada de `docs/Oscar/auth.service.numbered.txt`.
- 2026-05-06 12:18:10 Dev realiza: Añadida explicación línea a línea en `docs/Oscar/auth.service.js.md`.
- 2026-05-06 12:18:10 Dev realiza: Implementada auditoría de login en `backend/ldapts.js` — función `logLoginAttempt` y llamadas en el handler `POST /api/login` para `LOGIN_FAILED` / `LOGIN_SUCCESS` / `LOGIN_BAD_REQUEST`.
- 2026-05-06 12:18:10 Dev realiza: Verificación MD ↔ TXT: MATCH (sin diferencias). Verificado por extracción del bloque numerado y comparación exacta con `docs/Oscar/auth.service.numbered.txt`.

- 2026-05-06 12:30:00 Dev realiza: Añadido modelo Prisma `auditoriaaccesos` en `prisma/schema.prisma` para representar registros de auditoría de acceso.
- 2026-05-06 12:30:00 Dev realiza: `backend/ldapts.js` actualizado para insertar directamente en la tabla `auditoriaaccesos` (reemplazando el fallback a fichero). Si la inserción DB falla, se registra el error en consola.

Observaciones:
- La verificación se realizó extrayendo el bloque numerado del MD y comparándolo con `docs/Oscar/auth.service.numbered.txt`; no se detectaron diferencias.
- La auditoría de login intenta insertar en tablas de auditoría con nombres comunes y, si no existen, escribe un JSON de fallback en `log/login-audit-<timestamp>.json`.

---
Archivo(s) modificados:
- `docs/Oscar/auth.service.js.md`: insertado bloque numerado y explicación línea a línea.
- `backend/ldapts.js`: añadido `logLoginAttempt` y llamadas de registro en `/api/login`.
 - 2026-05-06 18:10:11 Dev realiza: Modificado `prisma/schema.prisma` para que `fecha_sistema` use `LOCALTIMESTAMP` (timestamp sin zona). Creada y aplicada migración `20260506181011_fecha_sistema_local`.
 - 2026-05-06 18:11:30 Dev realiza: Prueba POST `/api/login` con `DISABLE_TURNSTILE=1` -> registro en `auditoriaaccesos` comprobado; `fecha_sistema` contiene la hora local del sistema (no UTC).

2026-05-06 19:00:00
- Dev solicita: en `backend/ldapts.js` separar toda la lógica que no sea de ldapts y dejarla en `backend/index.js`.
- Dev realiza: **Refactor de separación de responsabilidades**:
  - `backend/ldapts.js` reducido a solo lógica LDAP pura: `buildPrincipalFormats` y `tryBind`. Exportados con `export function/export async function`. Eliminados: Pool, dotenv config, `logLoginAttempt`, `turnstileMiddleware`, imports `fs`/`path`/`dotenv`/`fileURLToPath`/`express`/`pg`/`cloudflare`. Ya no exporta un router.
  - `backend/index.js` recibe todo lo que salió de `ldapts.js`: importa `{ buildPrincipalFormats, tryBind }` desde `./ldapts.js`; Pool definido una sola vez; `turnstileMiddleware` consolidada en una sola definición; `logLoginAttempt` añadida; ruta `POST /api/login` movida aquí con la misma lógica de tipo 1 (LDAP) y tipo 2 (contraseña directa).
  - Verificado: `node --input-type=module --eval "import('./index.js')"` retorna "OK" + "Backend listening on port 4000" sin errores.

---
Archivos modificados:
- `backend/ldapts.js`: reescrito, solo `buildPrincipalFormats` y `tryBind`.
- `backend/index.js`: añadido pool único, `logLoginAttempt`, `turnstileMiddleware` consolidada, ruta `/api/login`.

2026-05-06 19:30:00
- Dev solicita: refactorizar `backend/index.js`, separar lo más posible en archivos propios según contexto, implementando un API gateway.
- Dev realiza: **Refactor API Gateway**:
  - Creado `backend/db.js`: Pool PostgreSQL singleton + `logLoginAttempt`. Carga dotenv.
  - Creado `backend/middleware/turnstile.js`: `turnstileMiddleware` como export nombrado.
  - Creado `backend/middleware/errorHandler.js`: `errorHandler` (4 parámetros Express) como export nombrado.
  - Creado `backend/routes/auth.js`: Router con `POST /api/login` y `POST /api/forgot-password`.
  - Creado `backend/routes/mail.js`: Router con `POST /api/send-email`.
  - `backend/index.js` reducido a API gateway puro: CORS, bodyParser, montaje de routers, error handler global, `app.listen`. Sin lógica de negocio.
  - Verificado: `node --input-type=module --eval "import('./index.js')"` → "Backend listening on port 4000" sin errores.

---
Archivos creados/modificados en refactor API gateway:
- `backend/db.js` (nuevo)
- `backend/middleware/turnstile.js` (nuevo)
- `backend/middleware/errorHandler.js` (nuevo)
- `backend/routes/auth.js` (nuevo)
- `backend/routes/mail.js` (nuevo)
- `backend/index.js` (reescrito)

2026-05-06 20:00:00
- Dev solicita: registrar cada error del backend en un JSONL diario en `backend/log/`.
- Dev realiza: **Implementación de error logging JSONL**:
  - Creado `backend/lib/errorLog.js`: función `logError(err, { context, req, status })`.
    - Genera `backend/log/errorYYYYMMDD.jsonl` (un archivo por día).
    - Cada línea es un objeto JSON con: `ts`, `context`, `message`, `code`, `status`, `stack`, `req` (method, url, ip, ua).
    - Escritura síncrona con `appendFileSync` para no perder registros en crashes.
    - Crea la carpeta `backend/log/` automáticamente si no existe.
  - Modificado `backend/middleware/errorHandler.js`: llama `logError` en caso 1 (JSON inválido, status 400) y caso 2 (error no manejado, status 500).
  - Modificado `backend/routes/auth.js`: llama `logError` en catch de consulta DB (`login-db-query`) y en catch de envío de correo (`forgot-password`).
  - Modificado `backend/routes/mail.js`: llama `logError` en catch de send-email antes del volcado de diagnóstico SMTP.
  - Verificado: `node --input-type=module --eval "import('./index.js')"` → "Backend listening on port 4000" sin errores.

---
Archivos creados/modificados en error logging:
- `backend/lib/errorLog.js` (nuevo)
- `backend/middleware/errorHandler.js` (modificado — agrega logError)
- `backend/routes/auth.js` (modificado — agrega logError en 2 puntos)
- `backend/routes/mail.js` (modificado — agrega logError en catch SMTP)

2026-05-06 19:36:00 Dev solicita: en `frontend/src/dashboard.tsx` genera un dashboard de ejemplo y documéntalo exhaustivamente.
2026-05-06 19:36:00 Dev realiza: Creado `frontend/src/dashboard.tsx` con componente de ejemplo (métricas mock, sparkline SVG y lista de actividad). Añadidos comentarios detallados en el código para cumplir la regla "documentalo todo".
2026-05-06 19:36:00 Dev realiza: Actualizado `frontend/src/dashboard.tsx.md` con el código fuente y explicación detallada.

Archivos modificados/creados:
- `frontend/src/dashboard.tsx` — dashboard de ejemplo y documentación en código.
- `frontend/src/dashboard.tsx.md` — documentación detallada generada/actualizada.

2026-05-06 16:16:20 Dev solicita: sube todo a https://github.com/claudionavarrojunaeb-wq/ssmm.git
2026-05-06 16:16:20 Dev realiza: Inicializado repo Git local, creado commit (5e8be29) y hecho push a `origin/main`.
  - Commit: `5e8be29` (root-commit)
  - Remoto: https://github.com/claudionavarrojunaeb-wq/ssmm.git (branch `main` configurado como upstream)

Archivos afectados: todos los cambios del workspace (dashboard, visor de logs, scripts de documentación, endpoints backend, documentación .md generada, logs y migraciones). Ver el commit para el listado completo.

2026-05-06 16:23:57 Dev solicita: añadir host `slimy-shore-vascular.ngrok-free.dev` a `server.allowedHosts` en `frontend/vite.config.ts`.
2026-05-06 16:23:57 Dev realiza: Actualizado `frontend/vite.config.ts` — `server.allowedHosts` ahora incluye `slimy-shore-vascular.ngrok-free.dev` junto a hosts locales (`localhost`, `127.0.0.1`, `::1`).

Archivo modificado:
- `frontend/vite.config.ts` — servidor de desarrollo configurado para aceptar el host ngrok.

2026-05-06 16:28:36 Dev solicita: deshabilitar temporalmente Turnstile para permitir login en desarrollo.
2026-05-06 16:28:36 Dev realiza: Añadida variable `DISABLE_TURNSTILE=1` en `backend/.env` y reiniciado el backend para que cargue la configuración. Turnstile queda temporalmente deshabilitado.

Archivo modificado:
- `backend/.env` — `DISABLE_TURNSTILE=1` añadido temporalmente.
2026-05-06 16:32:21 Dev realiza: Ocultar widget Turnstile en frontend. Se actualizó `frontend/src/Login.tsx` para no cargar ni renderizar Turnstile cuando `VITE_DISABLE_TURNSTILE=1`.
2026-05-06 16:32:21 Dev realiza: Añadida `VITE_DISABLE_TURNSTILE=1` en `frontend/.env` para desarrollo.
2026-05-06 16:38:01 Dev solicita: volver a habilitar Turnstile en frontend y backend.
2026-05-06 16:38:01 Dev realiza: Eliminadas las flags `DISABLE_TURNSTILE` y `VITE_DISABLE_TURNSTILE` en `backend/.env` y `frontend/.env` respectivamente. Reiniciado backend y verificación de Turnstile contra Cloudflare: respuesta válida (no skipped).
Archivos modificados:
- `backend/.env` — `DISABLE_TURNSTILE` eliminado.
- `frontend/.env` — `VITE_DISABLE_TURNSTILE` eliminado.
2026-05-06 16:45:12 Dev realiza: Commit de cambios de frontend/backend para correcciones de lint y tipos.
2026-05-06 16:45:12 Commit: fix(frontend): lint/type fixes Login, SendEmail, Dashboard (hash: 0a5ef02)
Archivos incluidos en el commit: Login.tsx, SendEmail.tsx, dashboard.tsx, frontend/.env, backend/scripts/check_turnstile.mjs, frontend/vite.config.ts, backend/log/error20260506.jsonl
