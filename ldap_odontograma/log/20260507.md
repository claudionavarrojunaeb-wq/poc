password reset email useremail login externo errores amigables 20260507 update
Resumen inicial: Implementación y corrección del flujo "Olvidé mi contraseña" — login por useremail, mensajes amigables.

2026-05-07 10:46:00 Dev solicita: Implementar flujo Olvidé mi contraseña (envío de código, verificación, reset, validación de complejidad e historial de 5 contraseñas).

2026-05-07 10:47:12 Modificado: `backend/routes/auth.js` — Añadidos endpoints:
- `POST /api/forgot-password` (genera código, lo almacena y envía por correo)
- `POST /api/forgot-password/verify` (valida código)
- `POST /api/forgot-password/reset` (cambia contraseña, valida complejidad e historial)
Se agregaron funciones auxiliares para crear tablas `password_reset_tokens` y `password_history` si no existen.

2026-05-07 10:47:40 Modificado: `frontend/src/Login.tsx` — Tras solicitar recuperación redirige a `/reset-password` para que el usuario ingrese el código.

2026-05-07 10:48:05 Añadido: `frontend/src/ResetPassword.tsx` — Componente con UI para verificar código y crear nueva contraseña. También se añadió `frontend/src/ResetPassword.tsx.md` con documentación básica.

2026-05-07 10:48:30 Modificado: `frontend/src/App.tsx` — Se añadió la ruta `/reset-password`.

2026-05-07 10:49:00 Observaciones:
- La validación de complejidad y la comprobación de las últimas 5 contraseñas se realiza en el backend.
- En este cambio las contraseñas continúan almacenándose en la columna `userpwd` como en la implementación original; se recomienda migrar a hash seguro en una siguiente tarea.

2026-05-07 Dev solicita: corregir flujo según consideraciones — el campo de login es useremail (no username), usertipo=1 → LDAP sin reset, usertipo=2 → externo con reset.

2026-05-07 Modificado: `backend/routes/auth.js` — Login y los tres endpoints de recuperación ahora buscan usuarios por `WHERE useremail = $1` en lugar de `WHERE username = $1 OR useremail = $1`.
Modificado: `frontend/src/Login.tsx` — Etiqueta "Usuario" → "Correo electrónico", tipo de input text→email, placeholder actualizado, mensajes de error amigables en español (mapa `FRIENDLY_ERRORS`).
Modificado: `frontend/src/ResetPassword.tsx` — Mismos cambios de etiqueta/placeholder/errores amigables. Mensaje "Error al resetear contraseña" → "Error al cambiar contraseña".

2026-05-07 Dev solicita: encriptar contraseñas con algo seguro, inquebrantable.

2026-05-07 Instalado: `argon2` en `backend/package.json` — argon2id (ganador PHC, recomendado NIST). Parámetros: memoryCost=65536 (64 MB), timeCost=3, parallelism=4.
Modificado: `backend/routes/auth.js` — Añadidas funciones `hashPassword` y `verifyPassword`. Login usertipo=2 usa argon2.verify con migración transparente desde texto plano (rehasheo en primer login exitoso). Endpoint /api/forgot-password/reset ahora hashea con argon2id antes de guardar en `users.userpwd` y en `password_history`; historial compara con argon2.verify soportando entradas legado.

2026-05-07 Dev solicita: actualizar Prisma porque cambió manualmente largo de userpwd (error VARCHAR(50) insuficiente para hash argon2id).

2026-05-07 Modificado: `prisma/schema.prisma` — `userpwd` de `VarChar(100)` → `VarChar(255)`. Se ejecutó `prisma db pull` para capturar estado real de la BD (detectó también tablas `password_history` y `password_reset_tokens` creadas directamente).
Creado: `prisma/migrations/20260507131200_argon2_pwd_tables/migration.sql` — documenta los cambios manuales: ALTER userpwd a VARCHAR(255) + CREATE TABLE IF NOT EXISTS para ambas tablas de contraseñas.
Ejecutado: `prisma migrate resolve --applied 20260507131200_argon2_pwd_tables` — resolvió drift sin resetear la BD. Estado final: "Database schema is up to date".

2026-05-07 Añadido: Interfaz de exploración del proyecto
- `frontend/src/verproyecto.tsx` — Nuevo componente para navegar el árbol del repositorio y ver archivos.
- `backend/routes/projectViewer.js` — Nuevo router con endpoints: `/api/project/list`, `/api/project/file`, `/api/project/download`.
Modificado: `backend/index.js` — montado el router en `/api/project`.

Observación: Estas herramientas están pensadas para entornos de desarrollo; exponer el árbol completo del repo en producción puede ser un riesgo de seguridad.

2026-05-07 14:15:30 Modificado: `backend/middleware/session.js`, `backend/package.json`, `backend/routes/auth.js`, `backend/index.js` — Implementación de sesiones basadas en JWT almacenadas en cookie HttpOnly:
- Añadido `cookie-parser` y `jsonwebtoken` como dependencias.
- `backend/middleware/session.js`: utilidades `signSession`, `setSessionCookie`, `clearSessionCookie` y middleware `requireAuth` para validar JWT en cookie `ssmm_session`.
- `backend/routes/auth.js`: se emite cookie HttpOnly tras login exitoso (externo y LDAP). Se añadieron endpoints `GET /api/session` (valida sesión) y `POST /api/logout` (limpia cookie).
- `backend/index.js`: activado `cookieParser()` y `cors` con `credentials: true`; protegido el router `projectViewer` con `requireAuth`.

2026-05-07 14:15:30 Modificado: `frontend/src/ProtectedRoute.tsx`, `frontend/src/Login.tsx`, `frontend/src/ResetPassword.tsx`, `frontend/src/SendEmail.tsx`, `frontend/src/VerLog.tsx`, `frontend/src/verproyecto.tsx`, `frontend/src/dashboard.tsx` — Ajustes cliente para usar sesión server-side:
- `ProtectedRoute.tsx`: ahora valida la sesión llamando `GET /api/session` (con `credentials: 'include'`).
- `Login.tsx`: ya no persiste sesión en `localStorage`; añade `credentials: 'include'` en `fetch` de login y en reintentos directos al backend.
- `ResetPassword.tsx`, `SendEmail.tsx`, `VerLog.tsx`, `verproyecto.tsx`: todas las llamadas `fetch` relevantes usan ahora `credentials: 'include'`.
- `dashboard.tsx`: logout llama `POST /api/logout` y redirige a `/login`.

2026-05-07 14:15:30 Pruebas rápidas: `curl -i http://127.0.0.1:4000/api/session` y `curl -i http://127.0.0.1:4000/api/project/list` devolvieron `401` sin cookie, confirmando que las rutas están protegidas.

Recomendación operativa: establecer `JWT_SECRET` seguro en entorno de producción, configurar `NODE_ENV=production` para activar `secure:true` en cookies, y revisar `sameSite`/CSRF si la app requiere mutaciones cross-site.

2026-05-07 14:45:00 Modificado: Implementación de refresh tokens y revocación en logout.
- Añadido `backend/middleware/session.js` funciones para generar, almacenar (hash) y rotar refresh tokens en la tabla `refresh_tokens`.
- `POST /api/refresh` añadido en `backend/routes/auth.js` para intercambiar refresh por nuevo access token y rotar el refresh.
- `POST /api/logout` ahora revoca el refresh token en BD y limpia cookies de sesión y refresh.

Notas de seguridad adicionales:
- El servidor aborta el arranque si `NODE_ENV=production` y `JWT_SECRET` no es un valor fuerte (evita despliegues inseguros).
- Por defecto `sameSite` queda `lax` en desarrollo y `none` en producción (configurable con `SESSION_SAME_SITE`).
- Evaluar protección CSRF si se permiten mutaciones desde orígenes cross-site; la estrategia actual (HttpOnly + SameSite) reduce riesgo, pero la defensa completa puede requerir tokens CSRF o double-submit cookies.

2026-05-07 16:20:00 Modificado: Implementación CSRF (double-submit) y documentación
- Añadido: `backend/middleware/csrf.js` — generación de token CSRF, set/clear cookie `ssmm_csrf` (no HttpOnly) y middleware `verifyCsrf` para validar `X-CSRF-Token` en peticiones mutantes.
- Modificado: `backend/routes/auth.js` — al hacer login y al rotar refresh se establece `ssmm_csrf`; `/api/refresh` y `/api/logout` ahora requieren verificación CSRF y limpian/rotan el token según corresponda.
- Modificado: `backend/.env.example` — forzado `SESSION_SAME_SITE=none` y aclaración sobre uso en producción.
- Modificado: `backend/README.md` — documentación de despliegue, generación de `JWT_SECRET` y notas sobre CSRF.
- Modificado: `frontend/src/csrf.ts`, `frontend/src/ProtectedRoute.tsx`, `frontend/src/SendEmail.tsx`, `frontend/src/dashboard.tsx` — lectura de cookie `ssmm_csrf` y envío del header `X-CSRF-Token` en peticiones mutantes (`/api/refresh`, `/api/send-email`, `/api/logout`).

2026-05-07 17:35:00 Modificado: `frontend/tsconfig.app.json` — Añadidos tipos `vite/client`, `react`, `react-dom` para asegurar que TypeScript cargue las declaraciones de `@types/react` y el namespace `JSX`.

2026-05-07 17:35:00 Modificado: `frontend/src/verproyecto.tsx` — Eliminada la anotación de tipo de retorno `: JSX.Element` (se permite inferencia) para evitar el error TS2503 cuando el namespace `JSX` no estaba resolviéndose en el contexto del editor/tsserver.

2026-05-07 18:12:00 Dev solicita: "si, integralos" — integrar Microsoft Graph OAuth y selector de contactos desde Outlook en el flujo de envío de email.

2026-05-07 18:12:30 Accion: Creado `backend/routes/graph.js` con endpoints: `/api/graph/connect`, `/api/graph/callback`, `/api/graph/status`, `/api/graph/contacts`. Implementa intercambio de código por tokens, almacenamiento en `ms_tokens` y refresh automático.

2026-05-07 18:13:00 Accion: Montado `graphRouter` en `backend/index.js` en la ruta `/api/graph`.

2026-05-07 18:13:30 Accion: Actualizado `frontend/src/verproyecto.tsx`: añadido botón para conectar/seleccionar Outlook, flujo de popup OAuth (postMessage), comprobación de estado `/api/graph/status`, petición `/api/graph/contacts` y modal para seleccionar contactos para el campo "Para".

2026-05-07 18:14:00 Notas: Requiere variables de entorno `MICROSOFT_CLIENT_ID`, `MICROSOFT_CLIENT_SECRET`, `MICROSOFT_REDIRECT_URI` y registro de la app en Azure AD. Los endpoints requieren sesión autenticada (middleware `requireAuth`).

